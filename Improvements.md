Phase 1: Improvements & Future Work
    Add semantic processing for non-TCP/UDP protocols like ICMP.
    Parallelize the data processing pipeline to run faster on multi-core CPUs.
    Add a device role identification feature by merging with an external inventory file.
    Implement deeper semantic analysis using Sentence-Transformers on packet payloads.